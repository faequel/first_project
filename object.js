const cat = {
    name: "mimi",
    model: 500,
    weight: "15kg",
    color: "brown",
    start: () => {
        return console.log(car.color + car.model);
    },
};

console.log(cat.name);
cat.start();